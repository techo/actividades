# Copiá este archivo a .deploy.env y completá con las credenciales reales.
# .deploy.env está gitignoreado — nunca se commitea.
export N8N_WEBHOOK_URL="https://n8n.techo.org/webhook/deploy-notification"
export N8N_WEBHOOK_TOKEN="pedile-el-token-a-quien-administra-n8n"
